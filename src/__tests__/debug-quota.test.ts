import { describe, it, expect } from 'vitest';

// Scratch file from investigating jsdom Storage mocking (see packet-0002 summary).
// Sandbox blocked file deletion — please `rm` this file manually.
describe.skip('debug-quota (scratch, safe to delete)', () => {
  it('noop', () => {
    expect(true).toBe(true);
  });
});
